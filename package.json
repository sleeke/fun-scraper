{
  "name": "fun-scraper",
  "version": "1.0.0",
  "description": "PWA for discovering live events in Vancouver",
  "private": true,
  "scripts": {
    "start:api": "npm --prefix backend start",
    "start:web": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend install --legacy-peer-deps && npm --prefix frontend run build",
    "test": "npm --prefix backend test",
    "install:all": "npm --prefix backend install && npm --prefix frontend install"
  },
  "dependencies": {
    "@vercel/blob": "^2.3.2",
    "axios": "^1.13.6",
    "better-sqlite3": "^12.8.0",
    "cheerio": "^1.2.0",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^4.22.1",
    "express-rate-limit": "^8.3.1"
  }
}
